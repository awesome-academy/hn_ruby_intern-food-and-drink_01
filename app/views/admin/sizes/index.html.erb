<div class="account-manage-page">
  <%= link_to t(".new"), new_admin_size_path, class: "btn btn-small btn-success" %>
  <table class="rwd-table">
    <thead>
      <tr>
        <th><%= t ".code" %></th>
        <th><%= t ".size" %></th>
        <th><%= t ".action" %></th>
      </tr>
    </thead>
    <tbody>
      <%= render @sizes %>
    </tbody>
  </table>
  <%== pagy_nav(@pagy) %>
</div>
